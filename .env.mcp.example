# Configuração do Prisma MCP Server
# Copie este arquivo para .env.mcp e preencha com suas URLs de banco

# Ambiente LOCAL (desenvolvimento)
MCP_DATABASE_URL_LOCAL="postgresql://usuario:senha@localhost:5432/projeto_igreja?schema=public"

# Ambiente STAGING (homologação)
MCP_DATABASE_URL_STAGING=""

# Ambiente PRODUCTION (produção - USE COM CUIDADO!)
MCP_DATABASE_URL_PRODUCTION=""

# IMPORTANTE:
# - O servidor MCP usa estas variáveis separadas das variáveis do Next.js
# - Recomendamos usar usuários de banco com permissões somente leitura
# - Para produção, considere usar um usuário ainda mais restrito
# - Nunca commite o arquivo .env.mcp no git (já está no .gitignore)
